---
- name: Implementing tags
  hosts: all
  tasks:
    - name: Create a user
      user:
        name: zabi
        password: bhai
      tags: user_creation
    - name: Install tree
      apt:
        name: tree
        state: present
        update_cache: yes
      tags: tree_installation
    - name: copy passwd file
      copy:
        src: /etc/passwd
        dest: /tmp
...
